/*
long len(string s)
{
    long i = 0;
    char ch = s[0];
    while (ch is not 0)
    {
        ch = s[i];
        i = i + 1;
    }

    return i;
}
*/

int test()
{
    int a = 5;
    string s = "Hello, world!";

    if (a is not 5) {
        a = 10;
    }
    else {
        a = s;
        a = a * 4;        
    }

    int size = 5;
    int+ numbers = create 10;

    // print "\"%s\" (%d characters)", s, len(s);

    numbers[0] = numbers[1];
    a = numbers[a];

    destroy numbers;
}
