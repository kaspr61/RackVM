/*
long len(string s)
{
    long i = 0;
    char ch = s[0];
    while (ch is not 0)
    {
        ch = s[i];
        i = i + 1;
    }

    return i;
}
*/

int test()
{
    int a = 250 + 7 * (4 + test() / 30);
    int+ list = create 1;
    list[0] = 10;
}
